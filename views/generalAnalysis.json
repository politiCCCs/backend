{
  "_id": "_design/generalAnalysis",
  "_rev": "48-9fa2b81bdee6108cba08c7021984d7d6",
  "views": {
    "emoji_is_political_ave_no_polis": {
      "reduce": "function(keys, values, rereduce) {\n    if (!rereduce) {\n        var length = values.length;\n        return [sum(values) / length, length];\n    } else {\n        var length = sum(values.map(function(v){return v[1]}));\n        var avg = sum(values.map(function(v){\n            return v[0] * (v[1] / length);\n        }));\n        return [avg, length];\n    }\n}",
      "map": "function (doc) {\n      //General stream\n  if (doc.regular_stream===true){\n  //([is_political, emoji length(int))\n  if (doc.is_political){\n    emit([\"political\"], doc.emojis.length);\n  }else if (doc.is_political===false){\n    emit([\"not_political\"], doc.emojis.length);\n  }\n  \n}}"
    },
    "emoji_is_political_ave_ALL": {
      "reduce": "function(keys, values, rereduce) {\n    if (!rereduce) {\n        var length = values.length;\n        return [sum(values) / length, length];\n    } else {\n        var length = sum(values.map(function(v){return v[1]}));\n        var avg = sum(values.map(function(v){\n            return v[0] * (v[1] / length);\n        }));\n        return [avg, length];\n    }\n}",
      "map": "function (doc) {\n  //ALl tweets\n  if (doc.is_political){\n    emit([\"political\"], doc.emojis.length);\n  }else if (doc.is_political===false){\n    emit([\"not_political\"], doc.emojis.length);\n  }\n  \n}"
    },
    "emoji_is_political_count_ALL": {
      "reduce": "function(keys, values, rereduce) {\n    if (!rereduce) {\n        var length = values.length;\n        return [sum(values) / length, length];\n    } else {\n        var length = sum(values.map(function(v){return v[1]}));\n        var avg = sum(values.map(function(v){\n            return v[0] * (v[1] / length);\n        }));\n        return [avg, length];\n    }\n}",
      "map": "function (doc) {\n  //([is_political, emoji length(int))\n  if (doc.is_political){\n    emit([\"political\"], doc.emojis.length);\n  }else if (doc.is_political===false){\n    emit([\"not_political\"], doc.emojis.length);\n}}"
    },
    "emoji_lab_lib_ave_ALL": {
      "reduce": "function(keys, values, rereduce) {\n    if (!rereduce) {\n        var length = values.length;\n        return [sum(values) / length, length];\n    } else {\n        var length = sum(values.map(function(v){return v[1]}));\n        var avg = sum(values.map(function(v){\n            return v[0] * (v[1] / length);\n        }));\n        return [avg, length];\n    }\n}",
      "map": "function (doc) {\n  if (doc.is_labor) {\n  key1 = 'labor';\n  emit([key1], doc.emojis.length);\n} else if (doc.is_liberal){\n  key1 = \"liberal\";\n  emit([key1], doc.emojis.length);\n}\n}"
    },
    "sentiment_is_political_ave_no_polis": {
      "reduce": "function(keys, values, rereduce) {\n    if (!rereduce) {\n        var length = values.length;\n        return [sum(values) / length, length];\n    } else {\n        var length = sum(values.map(function(v){return v[1]}));\n        var avg = sum(values.map(function(v){\n            return v[0] * (v[1] / length);\n        }));\n        return [avg, length];\n    }\n}",
      "map": "function (doc) {\n    if (doc.regular_stream===true){\n  //([is_political, emoji length(int))\n  if (doc.is_political){\n    emit([\"political\"], doc.sentiment_score);\n  }else if (doc.is_political===false){\n    emit([\"not_political\"], doc.sentiment_score);\n  }\n}\n}"
    },
    "sentiment_is_political_ave_ALL": {
      "reduce": "function(keys, values, rereduce) {\n    if (!rereduce) {\n        var length = values.length;\n        return [sum(values) / length, length];\n    } else {\n        var length = sum(values.map(function(v){return v[1]}));\n        var avg = sum(values.map(function(v){\n            return v[0] * (v[1] / length);\n        }));\n        return [avg, length];\n    }\n}",
      "map": "function (doc) {\n  //([is_political, emoji length(int))\n  if (doc.is_political){\n    emit([\"political\"], doc.sentiment_score);\n  }else if (doc.is_political===false){\n    emit([\"not_political\"], doc.sentiment_score);\n  }\n}"
    },
    "sentiment_lab_lib_greens_ave_no_polis": {
      "reduce": "function(keys, values, rereduce) {\n    if (!rereduce) {\n        var length = values.length;\n        return [sum(values) / length, length];\n    } else {\n        var length = sum(values.map(function(v){return v[1]}));\n        var avg = sum(values.map(function(v){\n            return v[0] * (v[1] / length);\n        }));\n        return [avg, length];\n    }\n}",
      "map": "function (doc) {\n  if (doc.regular_stream===true){\n  if (doc.is_labor) {\n  key1 = 'labor';\n  emit([key1],doc.sentiment_score);\n} else if (doc.is_liberal){\n  key1 = \"liberal\";\n  emit([key1],doc.sentiment_score);\n}else if (doc.is_greens){\n  key1 = \"greens\";\n  emit([key1],doc.sentiment_score);\n}\n}}"
    },
    "vulgar_is_political_count_ALL": {
      "reduce": "_count",
      "map": "function (doc) {\n  if (doc.vulgarity) {\n  key2 = 'vulgar';\n} else {\n  key2 = \"not-vulgar\";\n}\n  if (doc.is_political) {\n  key1 = 'political';\n  emit([key1,key2], 1);\n  }\n  else if (doc.is_political===false) {\n  key1 = 'not-political';\n  emit([key1,key2], 1);\n  }\n} \n"
    },
    "vulg_lab_lib_greens_count_ALL": {
      "reduce": "_count",
      "map": "function (doc) {\n   if (doc.vulgarity) {\n  key2 = 'vulgar';\n} else {\n  key2 = \"not-vulgar\";\n}\n  if (doc.is_labor) {\n  key1 = 'labor';\n  emit([key1,key2], 1);\n  \n} else if (doc.is_liberal){\n  key1 = \"liberal\";\n  emit([key1,key2], 1);\n} else if (doc.is_greens){\n  key1 = \"greens\";\n  emit([key1,key2], 1);\n}\n  \n}"
    },
    "sentiment_lab_lib_greens_AVE_all": {
      "reduce": "function(keys, values, rereduce) {\n    if (!rereduce) {\n        var length = values.length;\n        return [sum(values) / length, length];\n    } else {\n        var length = sum(values.map(function(v){return v[1]}));\n        var avg = sum(values.map(function(v){\n            return v[0] * (v[1] / length);\n        }));\n        return [avg, length];\n    }\n}",
      "map": "function (doc) {\n  if (doc.is_labor) {\n  key1 = 'labor';\n  emit([key1],doc.sentiment_score);\n} else if (doc.is_liberal){\n  key1 = \"liberal\";\n  emit([key1],doc.sentiment_score);\n}else if (doc.is_greens){\n  key1 = \"greens\";\n  emit([key1],doc.sentiment_score);\n}\n}"
    }
  },
  "language": "javascript"
}